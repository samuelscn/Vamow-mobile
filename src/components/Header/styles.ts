import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: 'red',
        width: '100%'
    },

    teste: {
        marginTop: 50,
        color: 'white'
    },
});

export default styles;